[build]
builder = "dockerfile"
dockerfilePath = "reflex_app/Dockerfile"
# Build context is the repo root so COPY can reach reflex_app/
dockerContext = "."

[deploy]
healthcheckPath = "/ping"
healthcheckTimeout = 120
restartPolicyType = "on_failure"

[[services]]
name = "cura-reflex"
